Language: Python3
Solution: Hashtable

class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        prevMap = {} # Create a hashtable
        
        for i, n in enumerate(nums): # assign number
            diff = target - n
            if diff in prevMap:
                return [prevMap[diff],i] 
            prevMap[n] = i
            
        return
        
Hashtable:
Using a function turns the 'Key' of a dictionary into the 'Index' of an array.
